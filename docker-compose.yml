version: '3.8'
services:
  api:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - SCRAPE_INTERVAL_MINUTES=30
      - DATABASE_PATH=/app/data/pcprice.db
    restart: unless-stopped
